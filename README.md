# School Management — Frontend (Next.js)

## Stack

- Next.js (App Router)
- TailwindCSS
- shadcn/ui

## Local development

1. Install deps
2. Create `.env.local`
3. Run dev server

## Environment variables

- `NEXT_PUBLIC_API_BASE_URL` — Base URL of backend API (FastAPI)

